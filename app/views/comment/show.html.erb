<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commentaire</title>
</head>
<body>
    <h1 style="text-align: center;"> Commentaire de <%= User.find_by(id: @comment.user_id).first_name %> </h1>
    <p style="text-align: center;"> <%= @comment.content %> </p>
    <p style="text-align: center;"><a href="<%=edit_comment_path(@comment.id)%>">Modifier</a> <br>
    <a href="<%=comment_path(@comment.id) %>" class="btn btn-danger" data-confirm="êtes vous sur ?" data-method="DELETE" >Supprimer</a></p>
    
</body>
</html>